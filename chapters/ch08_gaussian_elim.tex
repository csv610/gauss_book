\section{Introduction}

Gaussian elimination is one of the fundamental algorithms of linear algebra. Gauss developed it for solving systems of linear equations arising from geodetic surveying and least squares problems. The method systematically eliminates variables to reduce a system to triangular form.

In this chapter, we cover:
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\end{itemize}

\parameter
\parameter
For $Ax = b$ with $A \in \R^{n\times n}$:
\parameter
    \parameter
        \parameter
            \parameter
            \parameter
        \parameter
    \parameter
\parameter
\parameter
\parameter
The standard Gaussian elimination algorithm performs:
\parameter
    \parameter
    \parameter
    \parameter
\parameter
\parameter
For $m$ right-hand sides, the LU factorization costs $\frac{2n^3}{3}$ flops once, then each solve costs $2n^2 - \frac{2n}{3}$ flops. For sparse matrices, specialized algorithms (e.g., sparsity-preserving permutations) can dramatically reduce the count, though fill-in may complicate the analysis.

\parameter
\parameter
Gaussian elimination with partial pivoting is \parameter: the computed solution $\hat{x}$ satisfies $(A + \delta A)\hat{x} = b + \delta b$ with $\|\delta A\|_\infty \leq \gamma_n \|A\|_\infty$ and $\|\delta b\|_\infty \leq \gamma_n \|b\|_\infty$, where $\gamma_n = \frac{nu}{1 - nu}$ and $u$ is the machine precision.

However, the \parameter} $\rho = \max_{i,j,k} |A_{ij}^{(k)}| / \max_{i,j} |A_{ij}|$ can be as large as $2^{n-1}$ in pathological cases (Wilkinson, 1961). In practice, $\rho$ is typically $O(n^{1/3})$ for random matrices. The error bound is:
\[
\parameter}
\parameter
where $\parameter} = \|A\|_\infty \|A^{-1}\|_\infty$ is the condition number.

\parameter}: Partial pivoting controls the growth factor for most practical matrices. Complete pivoting (searching both rows and columns) gives $\rho \leq n^{1/2}$ but at twice the overhead and no proven advantage in practice.

\parameter
\parameter
$A = LU$ where $L$ is unit lower triangular and $U$ is upper triangular. With partial pivoting: $PA = LU$.

\parameter
\parameter
For $A$ symmetric positive definite: $A = LL^T$ with $L$ lower triangular. This is numerically stable without pivoting and requires only $\frac{n^3}{3}$ flops.

\parameter
\parameter
$A = QR$ with $Q$ orthogonal and $R$ upper triangular. Using Householder reflections $H = I - 2uu^T$. This is more stable than LU for ill-conditioned systems and is the preferred method for least squares problems.

\parameter
\parameter
Gaussian elimination has several important limitations:
\parameter
    \parameter
    \parameter
    \parameter
    \parameter
\parameter
\parameter
In \parameter:

\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
\parameter
    \parameter
\parameter
\end{itemize}
